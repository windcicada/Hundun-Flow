# 版本演进

产品仓库从独立的公开历史开始。版本号描述公开产品接口和能力，不沿用内部开发阶段名称。

版本变化遵循以下原则：

- 修订号：修复错误，不改变受支持的配置和接口语义；
- 次版本号：增加向后兼容的能力、字段或诊断；
- 主版本号：允许明确记录并提供迁移说明的不兼容变化。

JSON、Restart、diagnostics 和插件各自带格式版本。产品版本升级不会自动使所有旧二进制文件可读；是否兼容以对应 schema 文档和程序校验结果为准。
