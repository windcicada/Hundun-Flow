<!-- SPDX-License-Identifier: Apache-2.0 -->

# Re=3900 COAST equivalence capability inventory

Status: `REMEDIATED_CANDIDATE_BOUND`

This is a read-only inventory against the frozen
`HUNDUN_V04_COAST_EQUIVALENCE_V2` rule. It does not alter the rule, seal an
equivalence receipt or authorize pairing. The Luna inventory was reviewed
against the decisive COAST source and input locations by the main agent.

## Read-only source identity

- repository: `/home/wyf/code_dev/Coast_software`;
- branch: `master`;
- HEAD: `c4c1f788c0ca7359653a5acd621dd355bb5c7db3`;
- index tree: `33040b8195b88fe6427df741f56407e0e4a4cd78`;
- `git status --porcelain=v1` SHA-256:
  `02b971b4b6277cf0ace748839d1a452e1f701c670fa45f40c0f93482f08025c1`.

The COAST tree was already heavily dirty, including Stage 5/combustion work.
Status identity was unchanged before and after the worker inventory and main
review. No COAST/HUNDUN file was edited by the worker, and no build, COAST run,
MPI launch or process termination was performed.

## Frozen-field matrix

| Field group | Result | Decisive evidence |
|---|---|---|
| low-Mach EOS/enthalpy/`drho_dp` | `PARTIAL` | `SRC.Coast/densty.F90:45-140` has a compressible local-pressure density path and `drhodp=den/press`, but pressure clamp and temperature fallback alter the state; `module_arrays.F90:39-42` and `app/coast_legacy_driver.F90:1722-1728` expose only one density history |
| full BDF density defect and BE/BDF2 | `MISSING` | no second accepted density history or BDF2 assembly was found; the per-step path copies only `rhold=rho` and updates `drhodt` |
| inert sensible-enthalpy case | `PARTIAL` | the source supports sensible enthalpy when PDF/burn are disabled, but current `EXEC/input.d:57-70` enables burn and PDF |
| matched cylinder mesh/IBM | `PARTIAL` | generic STL/IBM paths exist; historical manifests bind `480x480x48` and the required domain, but current `EXEC/ibm_mesh.d` and current cylinder STL are absent, and no matching active-mask receipt exists |
| matched boundaries/Vreman wall | `PARTIAL` | boundary kinds and wall functions exist in `boundary2.F90`, `boundary3.F90`, `wall.F90` and `physics/coast_ibm_operators.F90`; the current input is not the frozen y/z symmetry case and constants/clipping/thermal hashes are unresolved |
| native SIMPLE work | `PARTIAL` | `SRC.Coast/input.F90:74` reads `niter`; `app/coast_legacy_driver.F90:1691-1700` executes state update plus pressure correction `niter` times; current `EXEC/input.d:30` is 2, but no baseline selection authority is frozen |
| fixed `dt=0.006` | `MISSING` | `app/coast_legacy_driver.F90:1769-1785` uses compile-time constant dt or an adaptive CFL update; the historical timing record does not establish a promotable matching input |
| canonical FP64 true residual | `MISSING` | `cgsol.F90:41-42` and `cgstab.F90:48-52` use FP32 work arrays; their terminal measures are normalized/scaled maxima (`cgsol.F90:307-323`, `cgstab.F90:452-474`); `module_coast_pressure_solver.F90:606-634` recomputes a scaled FP32 max, not the frozen global FP64 L2 `b-Ax` |
| frozen pressure tolerance/cap | `MISSING` | current and historical inputs use `1e-4`; the frozen rule requires `atol=1e-8`, `rtol=1e-6`, cap 500 and a scaling pullback |
| per-step solve evidence | `PARTIAL` | `module_coast_performance_monitor.F90:200-231` retains only the latest pressure record; per-equation solve counts, canonical initial/final residuals, termination reason and cap-hit evidence are absent; `module_coast_pressure_solver.F90:84-155` may fall back instead of fail closed |
| common terminal evaluator | `MISSING` | no evaluator matching active-cell EOS, full-BDF continuity, closed mass and gauge/outlet closure was found; `checkmass.F90` is species renormalization and the IBM diagnostic uses a one-history local density rate |
| max-rank hot-step timer | `MISSING` | `app/coast_legacy_driver.F90:1673-1677` starts a master-only global timer; the per-step `cpu_time` ends before later native work; `module_coast_performance_monitor.F90:334-375` measures summary intervals and gathers rank data rather than reducing the frozen step boundary with `MPI_MAX` |
| output-disabled immutable baseline | `MISSING` | output/restart remain in `app/coast_legacy_driver.F90:2508-2513,2635-2661`; current source/input/binary/tree are not a matching immutable case |

## Isolated-lane remediation review

The table above is the frozen discovery result against the original COAST
tree. It is retained verbatim as the reason the historical baseline was
rejected. The main agent subsequently implemented and reviewed a default-off
`re3900_equivalence` seam in the isolated worktree
`/home/wyf/code_dev/Coast_software_worktrees/re3900-equivalence`, based on HEAD
`c4c1f788c0ca7359653a5acd621dd355bb5c7db3`. The remediation is now committed
on `codex/re3900-equivalence-candidate` at HEAD
`3c22e0f029db1b2ca045ec9e212a95eacbcfe6a3`, tree
`ba449790e4918e7a3fd5c21e71c4e9f980a4691f`, with an empty
`git status --porcelain=v1 --untracked-files=all`. The candidate was rebuilt
with the frozen OCE sysroot and produced `EXEC/coast` SHA-256
`c5e483a0442b430e58040a8e04f5adf42b70b3595319c643e9265c87341a9bc4`;
the Re=3900 static contract passed after that rebuild.

| Former gap | Candidate result |
|---|---|
| local-pressure EOS, enthalpy and `drho_dp` | `RESOLVED_CANDIDATE`: fixed inert constant-`cp` closure, absolute local pressure, `rho=p/(RT)`, `drho_dp=1/(RT)`, fail-closed positive finite state |
| BE/BDF2 density defect | `RESOLVED_CANDIDATE`: two accepted histories, BE startup, BDF2 thereafter, full density-history storage and full pressure linearization |
| matched mesh and IBM | `RESOLVED_CANDIDATE`: correct face-coordinate mesh; direct COAST and authoritative HUNDUN cell masks are bytewise equal; derived active-face masks are bytewise equal |
| boundaries, Vreman and wall | `RESOLVED_CANDIDATE`: case/boundary validation passes; matched resolved values, anisotropic Vreman coefficient `0.07`, equilibrium shear constants and adiabatic cylinder receipt are hashed |
| native SIMPLE schedule | `RESOLVED_CANDIDATE`: exactly two native outer iterations; input `itnum` must equal the compile-time seam authority or the launch fails |
| fixed `dt` | `RESOLVED_CANDIDATE`: seam forces and validates `0.006`; BE/BDF2 coefficients derive from that fixed value |
| true residual and solver policy | `RESOLVED_CANDIDATE`: pressure is exact legacy ICCG with no fallback; the exact seven-coefficient physical operator is snapshotted before row scaling, the double-single high-plus-low solution is used by the pressure update and terminal residual, and FP64 reliable residual replacement runs every 128 pressure iterations; BiCGStab replaces from FP64 true residual every 8 iterations; all iterative equations use cap 500 and a cap hit rejects the step |
| per-step solve evidence | `RESOLVED_CANDIDATE`: the two-outer schedule records ten solves (`u,v,w,h,p` twice), iterations, initial/final true residuals, termination reason and cap hits |
| common terminal evaluator | `RESOLVED_CANDIDATE`: both products compile byte-identical ISO C source SHA-256 `0b70264bbc977447bbc1b2b84dfafc98570f67461fd954802907e5c93a410842` and apply the four common gates before commit |
| max-rank timer | `RESOLVED_CANDIDATE`: each rank uses `MPI_Wtime`; stop follows terminal audit and accepted-history commit; `MPI_MAX` occurs after the local stop; serialization is excluded |
| output-disabled immutable baseline | `RESOLVED_CANDIDATE`: output suppression covers the per-step schedule and finalizer; a fresh two-step run emitted no Visit, Restart, Log, Results or monitor files; clean source/tree, binary, full2/full20 input snapshot, MPI/CPU/affinity and performance policy identities are bound by the equivalence receipt |

The full common input, method and lifecycle receipt is
`v0.4-re3900-common-input-method-receipt.json`. Successful compilation and
static regression tests demonstrate that the seam is buildable. A fresh run
from the immutable candidate executed exactly two accepted steps (BE then
BDF2), recorded 20 linear solves plus two step records, and passed every
terminal gate. Its evidence is
`diagnostic_coast_frozen_3c22e0f_output_off_02/re3900_evidence.jsonl`, SHA-256
`a6bbc871ae5f8c62792bbdab45081d3ed6e296f2bb560ae86e132055c8b6944e`.
This is candidate validation, not a formal matched pairing result.

## Current nonmatching artifacts

```text
EXEC/input.d                 eaf48a765b672ecee1a4b99acb032a26762a401ce32dc2d091852e914efd6a67
EXEC/boundary_conditions.d   76f8237c7f6a384aeb72e95a5b731ea40fbeb9e0684b52d82ea63866955e117f
EXEC/ibm_mesh.d              MISSING
EXEC/coast                   5d373a4285130726758edee5ec51b3d6ba5d5b7a204fd98ea97ca13ba1f22257
COAST_SOURCE_VERSION.json    86273cc805bf7fa560f8f9a6224ebc06b979d8110291db599f4a0f38ff5e1bd7
BASELINE_MANIFEST.json       0c5ba28a18f078d37a277ff781026645de548daac3fb99e3c6ad5dda5b754634
```

The current input is reacting/PDF (`EXEC/input.d:57-70`) and therefore not
the comparison case. The current build metadata is preparation-only and does
not bind this dirty source tree as a performance baseline.

## Historical full-grid evidence is non-promotable

The closest historical case binds the required grid/domain, `niter=2` and
observed `dt=0.006`, but its `input.d:93-95` explicitly disables
compressibility. Its hashes are retained only for provenance:

```text
input.d                       fb98f5746f103c5b5794b3bcfcadc06edf4478498d9426150bb8051b413f1245
ibm_mesh.d                    d9910f7874fafcc308ec2cf5fbe9e72919742e2e5600e57684094cd2f32272a4
boundary_conditions.d        6c85fb60400df8df5d914f54d6c9d9ab96c5b1738a119ab9d9a520813bdb98ae
cylinder_re3900.stl           3a496f2c4f0c2b9b43c8eb0880bccb8788210afb1dd28167949970ae6fdcd837
mesh_generator_manifest.json  6f0b2cc90e688e9e433cf4b742aedbe166316de6b170bbd8419503e17bd4d17e
COAST binary                  4b65642997153520531a399edcc0ae89d98575522428e90da68e1e6e6590cb09
pressure monitor              d0377c26863b6eab510f2307f8263929843d79d33a041e0ced59849423c624b6
timing monitor                48ff16612cbac0c4205215cc5ec5b71d277f9f442afe76e4a000b7c85bb5886c
```

## Gate decision

The discovery failures remain historical evidence against the original dirty
COAST tree. The isolated candidate closes every frozen field without weakening
the HUNDUN equation scope, two-PISO schedule, true-residual rule, terminal
gates or timer boundary. Exact candidate, input and resource identities are
bound in `v0.4-re3900-coast-equivalence-template.json`; formal pairing becomes
authorized only when that document validates with `--require-sealed`.
