<!-- SPDX-License-Identifier: Apache-2.0 -->

# v0.4 literature data receipt

State: `R4 PARTIAL / complete=false; DIRECT TOTAL-DRAG AND PERIODIC-piD LIFT-RMS AUTHORITIES PENDING`

The four JSON authorities under `docs/references/` are accepted only after
`tools/v04_literature_extract.py validate` succeeds and an immutable receipt
binds their SHA-256 values, the extractor SHA-256, and any locally retained
source attachment/digitization file. Values and tolerances must be frozen
before reading HUNDUN-FLOW cylinder or backward-step results.

Required authorities:

- `cylinder-re3900-parnaudeau.json`: definitions and mean/fluctuation profiles
  at `x/D = 1.06, 1.54, 2.02` with source locator and extraction uncertainty.
- `cylinder-re3900-norberg.json`: force/Strouhal definitions, Reynolds-number
  applicability and reported experimental uncertainty.
- `cylinder-re3900-tadrist.json`: direct global fluctuating-lift apparatus,
  target-neighborhood envelope and explicit span/end/blockage limitations.
- `backward-step-driver-seegmiller.json`: NASA geometry, flow conditions,
  reattachment and registered profile/wall observables.

No approximate value from a secondary CFD paper may be promoted to an
experimental authority. If a primary table or machine-readable profile is not
available, the JSON must say that digitization remains pending; it must not
invent points to make the schema pass.

The current machine receipt is
`docs/verification/v0.4-literature-data-receipt-r4-partial.json`. It freezes
the independently audited Parnaudeau R3 profile arrays and bounds, the direct
and derived scalar authorities, and NASA dataset URLs/hashes. It deliberately
does not authorize the Task 21 `literature` gate: directly measured total mean
drag and finite-span lift RMS authorities applicable at Re=3900 remain
pending, including the scientific-work mapping to HUNDUN's periodic `pi D`
span.

The validator derives `complete=false` from that pending reference and checks
all local artifact hashes, including the exact extractor. A release workflow
must call:

```text
python3 tools/v04_literature_extract.py receipt-validate \
  --require-complete \
  docs/verification/v0.4-literature-data-receipt-r4-partial.json
```

The expected result today is rejection. Removing either force-pending row,
manually flipping the receipt flag, substituting a sectional or pressure-only
force coefficient, or taking a value from a secondary CFD table is not an
admissible way to pass. `required_observables` and `pending_observables` are
validated as exact complements, so deleting a pending row while its required
authority is absent fails closed.

The completed Parnaudeau path is additionally fail-closed in the extractor.
Deleting `pending_profiles` is insufficient: completion requires exactly the
registered 3 stations by 5 quantities, at least eight ordered points per
profile over a declared common transverse interval, exact figure/legend
locators, an acquired publisher/author/institution primary artifact, all
source/calibration/raw-trace artifacts bound by the machine receipt, and a
separate extraction-error bound for both coordinate and value. Controlled
digitization composes pixel/tick calibration, quantization, line/marker width
and independent-repeat differences conservatively; it never relabels that
bound as unreported experimental uncertainty.
The exact preregistered method is
`docs/verification/v0.4-parnaudeau-controlled-digitization-rule.md`; a
completed machine receipt must bind its SHA-256 alongside the source,
calibration and raw-trace artifacts.

## Frozen 2026 supplementary-dataset reconciliation

Before `doi:10.57745/DHJXM6` may replace controlled digitization of
Parnaudeau Figs. 11--15, a read-only research receipt must bind the official
Recherche Data Gouv V1 metadata and answer every row below without consulting
any HUNDUN result.  An absent or ambiguous row is `NOT_EQUIVALENT`; it may not
be filled from a secondary CFD paper.

| row | required authority |
|---|---|
| experiment identity | official metadata/readme must identify the same INRAE cylinder experiment or explicitly document how this dataset completes/reprocesses the Parnaudeau 2008 PIV data; record cylinder diameter, reference velocity, Reynolds number, blockage/tunnel geometry and PIV plane wherever supplied |
| coordinate identity | authoritative file metadata/header must define streamwise and transverse origins, axes and units; the mapping to paper stations `x/D=1.06,1.54,2.02` must be explicit and require no extrapolation |
| normalization identity | velocity and stress units or normalization must be explicit; conversion to `U_c`, `U_c^2` must use a source value fixed before any HUNDUN output is read |
| observable coverage | machine-readable mean `u`, mean `v`, `u'u'`, `v'v'` and `u'v'` must all cover each registered station and a common transverse interval; a missing quantity/station rejects substitution |
| extraction error | bind numeric precision, exact-grid selection or a predeclared interpolation rule, maximum station offset and the resulting extraction/interpolation error separately from experimental uncertainty |
| provenance | bind persistent ID, version, official file IDs/names/sizes/checksums, download timestamps, local SHA-256 values, license and the exact extraction command/script hash |

Only metadata, readme and published mean/Reynolds-stress products should be
downloaded during reconciliation; the approximately 12.3 GB instantaneous
archive is out of scope unless the published products are insufficient, in
which case this route remains incomplete pending a separately frozen raw-data
reduction protocol.  A successful research receipt authorizes the main agent
to design and review a small deterministic profile extraction.  It does not
itself edit the literature JSON, mark `complete=true`, select comparison
tolerances, authorize full20/long statistics or decide the final gate.

The frozen reconciliation completed at commit `c59a769` and is bound in
`docs/research/2026-08-22-parnaudeau-2026-piv-dataset-reconciliation.md`.
Its result is `NOT_EQUIVALENT`: the official V1 inventory exposes 998 raw
`Serie_*.txt` velocity fields but no README or published mean/Reynolds-stress
products, and the sampled authoritative header does not define the cylinder
origin, diameter, paper-station mapping or `U_c` normalization. The archive
was not downloaded. Therefore `doi:10.57745/DHJXM6` was not used to replace
the paper figures. A separately frozen exact-vector extraction from the
provenance-verified IRISA primary PDF subsequently completed the 15-profile
matrix at commit `1adbc99`; its immutable artifacts and independent R3 audit
are recorded in
`docs/verification/2026-08-22-v04-parnaudeau-r2-independent-audit-resolution.md`.
The receipt nevertheless remains `complete=false`, and long physical
statistics remain forbidden, because both direct force authorities above are
still pending.  The user later restored the original Task 20/evidence-schema
order, under which the independent full20 performance gate may proceed
without consulting HUNDUN physical statistics.

## Root-reviewed force-authority closure, 2026-08-22

The remaining force bundle was searched against four additional primary
routes without consulting HUNDUN statistical output.  None closes a required
observable:

- Wieselsberger NACA TN 84 is an official public primary scan and supports a
  direct whole-force drag method over a Reynolds-number range containing
  3900.  It has no printed Re=3900 value or uncertainty and its Figure 1 would
  yield only a derived digitization from long-cylinder data with unresolved
  blockage, end, support, roughness and inflow metadata.  The root agent did
  not preregister that digitization because even a successful extraction
  cannot close the force bundle.  The report and source SHA-256 values are
  `fe16e4ebc57cf8e81f6842ac75fb9cda0115ceabcef76394637b23a3495e92ec`
  and
  `92561c3402f473ded94440ef93229e0d75c6b197e1fcab28206a26b84e35814c`.
- The official Lund Open Access Norberg 1987 thesis, SHA-256
  `beb7b87242ac3d3693e6f9dd1990b38d289e8b87cd4892ed011c9718c7d37495`,
  was reviewed page by page.  It explicitly distinguishes present
  pressure-derived sectional drag from Wieselsberger whole-length total drag.
  Its present lift row is sectional (`ell/D -> 0`), pressure-derived and starts
  at `Re=10^4`; it supplies neither a Re=3900 total-force point nor signed
  spanwise covariance data for a periodic `pi D` mapping.  The consolidated
  audit SHA-256 is
  `829bc18ba4a8cd7255d9959ae311283612b163cc302d8ef86aa7c5007520028d`.
- Tritton 1959 is excluded by the publisher abstract itself: its direct-drag
  measurements cover only `Re=0.5--100`.  The negative audit SHA-256 is
  `6bd6ff566ab12a80221138b877c7b77dac36d4e6e3ac3a9d54f4aac73ccfa288`.
- Bishop--Hassan remains the method-level source whose stationary range
  `Re=3600--11000` covers the target and whose abstract identifies direct
  fluctuating lift/drag and steady mean drag.  Royal Society, JSTOR and other
  archival routes did not yield lawful readable paper pages in this
  environment.  The archival follow-up SHA-256 is
  `4f4321782b612a6b3ca61185b7fac0c05ed0d1c45e7dd661c306825e275a8045`;
  it adds no numerical, normalization, span, boundary or uncertainty
  authority.

These are fail-closed research receipts, not attachments that complete the
machine reference.  The R3 partial JSON remains byte-identical and
`complete=false`; `total_mean_drag_coefficient_re3900` and
`finite_span_lift_rms_coefficient_re3900` both remain pending.  A sectional
pressure coefficient, a compiled curve, a figure-derived long-cylinder value
or an inaccessible-paper abstract may not satisfy either row.

## Broader open direct-force scan, 2026-08-22

The root agent reviewed the complete eight-candidate scan at
`docs/research/2026-08-22-v04-re3900-open-direct-force-candidate-scan.md`,
SHA-256
`d6728fe3b05915d6197dc21745480d37ad166fe1b4a65d403e21ee49e398b5b5`.
No candidate supplies, in lawfully readable primary pages, the complete
combination of a target-Re direct total-force point, coefficient and RMS
definitions, active span/end conditions, blockage treatment and uncertainty.
In particular, Fukuoka et al. separates a `Re=4100` PIV campaign from its
three-axis-load-cell campaign at `Re=28000--317000`; it is explicit negative
evidence, not a target force datum.  Its official OA PDF SHA-256 is
`560e38492ab89349fa997f209cd5fdd34bc79f24740ceaaeea52c4a06a1933cc`.
The root visual review covered all nine rendered pages and the registered
pages 2--5.  `qpdf --check` reports malformed linearization hint-table lengths,
so the receipt records readable rendered content without asserting a clean
PDF container.

Bishop--Hassan 1964 and Tadrist et al. 1990 are the only scan entries whose
publisher metadata jointly place a direct/global force method across the
target Reynolds-number range.  Both remain `ACQUIRE`, not authority, because
complete apparatus, force-area, normalization, target-point and uncertainty
pages were unavailable.  Richter--Naudascher remains an unclassified
full-text follow-up; the other five routes are method-, Reynolds-number- or
boundary-condition exclusions.  The next admissible input is a lawfully
obtained complete primary paper for Bishop--Hassan
(`doi:10.1098/rspa.1964.0004`) and/or Tadrist et al.
(`doi:10.1063/1.857804`).  Until such pages pass root audit, the partial JSON
and both pending rows remain unchanged and no long-statistics launch is
authorized.  Full20 performance pairing is governed separately by the frozen
performance candidate and the restored original gate order.

The subsequent author/institution manuscript audit is frozen in
`docs/research/2026-08-22-v04-tadrist-bishop-author-manuscript-audit.md`,
SHA-256
`b31ced0882cb229e1b2e7093192c61ce6bc82d008f889f0d7dfcea91253d5a8b`.
HAL returned zero exact DOI and title records for Tadrist et al.; OpenAlex,
Unpaywall, Semantic Scholar and OpenAIRE exposed no repository full-text
location for either paper.  Publisher and UCL routes returned access
challenges or metadata/HTML shells rather than PDF bytes.  No source artifact
was acquired, so this is access provenance only and changes no pending field.

## User-authorized Tadrist primary PDF, 2026-08-22

The user supplied and explicitly authorized the lawful complete Tadrist et
al. 1990 PDF for accuracy validation.  The preserved read-only artifact is
`/home/wyf/code_dev/.benchmarks/hundun-flow-v0.4-re3900/literature/tadrist-1990-user-authorized-e62f0c6b/tadrist1990.pdf`,
543,517 bytes, SHA-256
`e62f0c6bfc2b2bea7e89de6ffb075eee0740bcc1ee393254e44c2bb84ab4e1d3`.
`qpdf --check` reports no syntax or stream errors, and the root agent reviewed
all eight rendered pages.  The page inventory SHA-256 is
`2997f0d298ffa75724401f3a6f20d37ab0ceac6ac690b5e06a2e8c37a90ed028`.

The paper establishes a direct two-end-sensor measurement of fluctuating lift
over the full tube length for `Re=3000--30000`.  It reports approximately
`C_z=0.02--0.08` over `Re=3000--7000`, upstream `Tu=5%`, full-spectrum force
variance, four aspect/blockage combinations and strong end/aspect sensitivity.
These are frozen as source facts in
`docs/references/cylinder-re3900-tadrist.json`; no graph digitization or
Re=3900 interpolation was performed.  The reported 7% full-band versus
midheight-band difference is a spectrum-band sensitivity, not experimental
uncertainty.  The paper's `lambda_c=4D` is adopted from other studies to derive
local lift and is not a Tadrist correlation-length measurement.

Therefore Tadrist is an admissible advisory target-neighborhood accuracy
constraint, but it does not establish periodic `pi D` span equivalence, an
exact Re=3900 value or total mean drag.  The new machine receipt is
`docs/verification/v0.4-literature-data-receipt-r4-partial.json`, SHA-256
`1dfdeb16b92f4a29a5dc2a0c4daa146499e8decb1af5df197b652203e36b2e3f`.
It binds the PDF, provenance receipt, artifact manifest, page inventory and
all prior R3 evidence.  It validates with `complete=false`; only
`cylinder-re3900-norberg.json` remains incomplete, so long physical statistics
and the `literature`/`final` gates remain forbidden.

## V04-2 numerical-admission boundary, 2026-08-30

The V04-2 numerical-product admission changes no literature authority,
required observable, tolerance or receipt completion state.  The active R4
receipt remains `complete=false`; `receipt-validate --require-complete`
therefore rejects as intended.  Long physical statistics and the
`literature`/`final` gates remain unauthorized.
