# v0.4 全范围检查与修复台账

基线：`148e3dc7de303b3b0b704e556d68b64012077c43`。日期：2026-09-05。
范围是默认构建的 v0.4 产品、公共接口、测试、根目录构建及运行/验证工具；
根目录旧源码和 v0.3 是非默认历史实现，不能用本轮 v0.4 测试声称它们已经验收。
“全范围检查”表示覆盖模块目录和关键合同，不表示逐条路径穷举或无缺陷证明。

## 方法和约束

- CodeGraphF 0.9.8 索引基线为 909 个文件、34,501 个节点、133,559 条边；
  查询结果用源码核对，空查询不作为符号不存在的证据。
  修改后已同步：913 文件、34,556 节点、132,806 边；pending changes 为 0，无 worktree mismatch。
- 测试公共接口已获用户确认：ApplicationService、ProductDriver、MPI writer 和已有数值算子入口。
- 先运行能失败的检查，再做局部修复；每个性能候选只测一轮。
- 不调用 ponytail；不改 Re3900 网格、物性、边界、固定 dt 或物理残差门槛。
- 本轮结果不是 V1.0 发布验收，不推送 GitHub，不覆盖历史运行目录或原 checkpoint。

## 基线实测

Release / Clang / libc++，不启用 fast-math。完整默认测试 206 项：199 通过、7 失败，墙钟 44.18 s。
完整输出：`/tmp/hundun-audit-148e3dc-20260905-ctest.log`。

```bash
LD_LIBRARY_PATH=/home/wyf/.local/opt/hundun-toolchain/clang/lib/x86_64-unknown-linux-gnu \
ctest --test-dir build-re3900-fix-test --output-on-failure -j8
```

| 检查 | 基线结果 | 当前判断 / 下一验证 |
| --- | --- | --- |
| `v04_solver_initialization_projection` | `6/9725`，独立 PCG oracle 成功 | 修复后通过：实际连续性接入 FGMRES 停止判断，容差不变 |
| `v04_product_fresh_projection` 及 MPI 1/2/4 | 重启字段/通量字节比较失败 | 4 项通过；确认是旧测试误要求 IBM 界面保留负零，不是速度被投影 |
| `v04_product_pressure_energy_temporal_convergence` | 三个 dt 均在 stage 44 返回 `2/5790` | 修复后通过：非 limited_central2 不绑定专用 compiled 缓存，也不进入依赖它的 prepared 路径 |
| `v04_literature_partial_receipt` | 原 checkout 的绝对路径不存在 | 历史证据不可静默改写；区分可移植测试与外部历史附件验收 |
| 新增 app 正常结束诊断检查 | 求解成功，但报告 `pressure_solve_calls=0` | 已确认尾部重建报告漏复制；局部修复后该检查通过 |
| 新增 app 输出失败报告检查 | 第一步已提交，但报告步号仍为 0 | 修复后通过：Visit/Restart/Evidence 错误均保留步号与时间，CLI 区分终止阶段 |
| 新增配置分配失败扫描 | 单 rank 注入 bad_alloc 使进程 SIGABRT | 去掉 valid_boundary 的 noexcept 后，1/2 rank 扫描和原配置/广播共 6 项通过 |
| 新增路径分配失败扫描 | 路径规范化/组件迭代导致 SIGABRT | 去掉错误的 noexcept；规范路径按分隔符边界比较，不再分配组件；前缀同名目录不能误判为子目录 |
| 新增 app 输出前分配失败 | 单 rank 返回导致 MPI_Allreduce 错序并中止 | 6 个阶段的本地构造在进入下一个 collective 前汇总错误，1/2/4 rank 全部通过 |
| 新增自适应对流时间尺度 | 均匀高速场从 0.1 缩到 0.05 后仍以 10211 停止 | 原离散 CFL 公式前置到 app，每步读已接受 rho/phi；1/2/4 rank 通过 |

### 已确认根因与修复边界

- 初始投影：锚定后的代数 L2 残差 `2.8534e-10` 已满足线性相对阈值，
  但去掉锚点约束后的实际通量散度最大值 `3.92249e-10` 仍超过物理要求。
  使用现有 `LinearConvergenceAudit`，仅在生产 FGMRES 接近停止时检查真实通量；
  独立 PCG oracle 保持独立，最终审计及状态的一致提交与回退不变。
- 重启：单 rank 的所有状态字段逐字节相同；192 个差异全部位于 IBM 界面，
  且只是 `-0.0 → +0.0`。没有更改生产重启算法。测试对非界面通量继续逐字节比较；
  MPI 重复界面允许两种零符号，但绝不允许非零穿透通量。
- `central2`：原先被错误地绑定到 limited_central2 专用缓存；只去掉缓存后还出现
  `2/5784`，因为 prepared Schur 路径也依赖该缓存。两处路由同时修正后原时间精度测试通过。
  当前 Re3900 的 limited_central2 路径不受此修复影响。
- 正常结束重建 ApplicationRunReport 漏掉末步求解记录；改为保留现有报告。
  每次提交后立即更新步号和时间，并添加输出/资源/推进终止阶段，不把写入失败当数值失败。
- `valid_boundary` 在 noexcept 中分配 unordered_set，导致异常绕过既有配置阶段 catch。
  分配失败扫描通过公共 CaseCompiler 逐点触发真实分配，验证全 rank 状态一致和失败时不发布模型。
- 路径比较：调试器栈定位到 `within` 的 filesystem path 组件迭代；本机 libc++ 的组件赋值
  也有 noexcept 分配。对已经规范化的绝对路径改用不分配的本机字符串比较，并检查分隔符边界；
  不绕过目录安全校验。路径规范化本身允许 bad_alloc 传播到公开入口的 Status 转换。
- app 的 Visit/Screen/Monitor/Restart/Evidence 路径和文本构造都纳入本地阶段 catch，再做全体状态汇总。
  `getrusage` 本地失败也先汇总。新测试通过真实 ApplicationService 入口，在指定 rank 注入 bad_alloc；
  原先报 MPI_Allreduce 错序，修复后六种阶段均一致返回，已提交步数仍为 1。
- 自适应对流时间尺度：`ProductDriver::constrain_convective_time_limit` 读取已接受密度、质量面通量
  和各单元实际体积，跳过固体单元。ApplicationService 在每步提出 dt 前调用；由现有 TimeController
  进行全局最小值和一次 CFL 缩放，无流出时不增加限制。保留显式 `advance(limits, report)` 接口，
  固定 dt 不扫描、不改变。新增公共接口测试覆盖速度缩放、零速度、更严格外部上限和 1/2/4 分区。
  **本次只补对流项**；黏性/热/组分/声学输入仍由现有调用者提供，不能宣称所有物理尺度已接入。

## 模块覆盖与待验证事项

| 模块 | 现有检查覆盖 | 审阅重点 / 状态 |
| --- | --- | --- |
| 配置、入口、初始化 | case/wire/CLI/init-case | 对流输入、边界/路径异常、末步报告已修；显式初始状态和混合物初始化仍需设计 |
| 网格、分区、IBM | Cartesian/STL/focus/decomposition/IBM order/MPI | 新投影停止与重启测试合同已修；不据此声称所有几何/分区均已验收 |
| 物性、输运、湍流 | thermo/transport/closed-mass/Vreman/WALE/wall | 保留 COAST-native 变量物性；本轮没有证据支持改成常系数 |
| 时间推进、事务 | BDF/time/retry/transaction/restart | BDF2 系数未改；对流前置、接受后报告已修，其余物理尺度和重试冷却待验证 |
| 压力—能量、动量、标量 | MMS/conservation/Schur/globalization/predictor | central2 路由已修；SIMPLE 后期空间后备路径越过重现故障点，见下表 |
| Krylov、MG、归约 | Krylov/MG reuse/odd partitions/line smoother | 已有批量归约和融合 basis update；multidot 内存扫描仍是待测优化，不先换 PCG |
| halo、donor、MPI 异常 | halo failure/writer/app allocation injection | 配置和 app 输出阶段注入通过；产品编译 arena 的所有本地失败路径尚未穷举 |
| arena、工作区、I/O | arena/hot-resource/restart/writer bytes | 上批消除 Visit 双大缓冲；不能代替全进程峰值预算或生命周期别名证明 |
| 构建、发布、验证工具 | 默认 CMake/evidence/literature | 预注册发布策略仍指向旧网格及 Evidence V7，当前输出 V8；不可当作新算例已验收 |

## Re3900 故障区间单轮试验

直接停止证据仍为 `6/10210`、stage 54、12 次 refinement 后能量 `1.22875e-6 > 1e-6`。
当前算例固定 dt、无输运标量；自适应常量输入和混排标量不是这次停止的解释。
本轮先从保留的第 500 步恢复 202 步，保存接近故障点的完整历史，再做短程检查。

- 作业：`hundun-re3900-audit-500to702-20260905.service`。
- 输出：`/home/wyf/code_dev/.benchmarks/hundun-piso-simple-product-20260903/trial-D0p02-zpi2-52/audit-148e3dc-500to702`。
- 固定基线 runner SHA-256：`2fc0dc66ab63d28b5b9aab4968d63474d7fda74116b49242937abeb297f0a6a0`。
- 这是 tests=ON 的诊断重放，不是发布/性能候选；同时进行的小测试和构建也使其耗时不适合正式性能排名。
- 重启会重置部分求解器缓存，不能未经比较就声称第 703 步重放逐比特一致。

第 500→702 步重放成功；从 702 单步重启的 703 成功，但用满 12 次 refinement。
从保留的 703 checkpoint 再运行，基线在 712 重现 `6/10210`、stage 54：
连续性 `4.18816e-7`，能量 `1.1312e-6`。这是同类停止，不是把故障步号假装成严格可重复。

| 候选（每个仅一轮） | 修改 / 结果 | 计算耗时与结论 |
| --- | --- | --- |
| baseline `audit-pre-c2-704to733` | 704–711 接受，712 用尽 12 次 refinement | 8 个接受步均值 15.6745 s；失败步 15.2037 s，stage 50 占 13.2179 s |
| α 近门槛限为 1，`audit-alpha-near-gate-704to733` | 在 704 就失败；E 降到 `5.088e-8`，但 C 为 `1.35633e-6` | 单纯抑制外推会让连续性收敛过慢；**已撤回**，不计为修复 |
| spatial fallback `audit-spatial-fallback-704to733` | C2 前 6 次 refinement 保留双对角；仍不收敛则用已有 IBM 空间 quasi-Newton 路径 | 704–733 全部通过；平均 17.2765 s/步，stage 50 平均 15.0720 s；平均 9.9333 次 refinement |

后备路径不增加 12 次上限，不放宽任何物理门槛，不改变最终候选筛选，也不宣称 IBM Jacobian 是精确全导数。
上述试验都在相同 703 checkpoint、456×256×52 网格、COAST-native 变物性、固定 dt 和 128 ranks 下进行。
由于运行期间有构建/小测试，并且基线在 712 终止、后备路径覆盖更长区间，耗时仅作诊断，不能给出正式提速比。

30 步后备路径的观测：

- 最大连续性 `9.96672845e-7`、最大能量 `9.86516877e-7`，原门槛均为 `1e-6`。
- 最大已提交对流 CFL `0.24083314 < 0.8`；没有重试、没有时间方法降级。
- 节点峰值 RSS 高水位从 28.724388 到 28.724915 GiB，约增加 0.54 MiB；这不是长期无泄漏证明。
- 最终 checkpoint：`audit-spatial-fallback-704to733/Restart`，step 733，time `0.010121932695361767 s`（t*=1.466）。
- 基线诊断 runner SHA-256 `46abb3b460123a88c189541efa7436651208d4f5782e3cf81cb9f3c9c6105f3f`。
- 被否定的 α 候选 SHA-256 `ae98194c8dea08c8bf084e25502d5ae1a9e6f5abf5320ab40bb8748e7c8cf6f5`。
- 空间后备候选 SHA-256 `b58a7982cd2febeb8b0fca2b0411d42cc9027b6eee63193c2f2be645960a1ef9`。

## 检查构建与验收范围

ASan/UBSan 需要 C 和 C++ 使用同一 Clang 工具链；本机旧 GCC C 与 Clang C++ 混合时 UBSan 运行库符号不兼容。
第一批 8 项检查通过（109.59 s）；第二批 10 项检查通过（94.18 s），两批有 3 项重叠。
使用 `ASAN_OPTIONS=detect_leaks=0:halt_on_error=1` 和 `UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1`。
外部 MPI 未作 LeakSanitizer 验收，不能把这组结果写成“无内存泄漏”。
日志分别为 `/tmp/hundun-audit-sanitize-20260905.log` 和 `/tmp/hundun-audit-sanitize-final-20260905.log`。
最后的公共接口调整另以 ASan/UBSan 复查 app 和 1/2/4 rank 对流时间尺度，4/4 通过（53.43 s），
日志 `/tmp/hundun-audit-sanitize-api-20260905.log`。

最终 Release 默认全套：**213/214 通过，49.11 s**。
唯一失败仍为基线已有的 `v04_literature_partial_receipt` 外部历史归档路径缺失；没有删除、跳过或改哈希。
最初 7 个基线失败中的 6 个数值/重启测试已通过；新增 8 个 MPI 回归全部通过。
新增回归已显式加入 focused correctness selector（候选身份随源码改变，不沿用旧发布验收）。
最终全套日志 `/tmp/hundun-audit-accepted-20260905-ctest.log`。
关键 RED/GREEN 与 sanitizer 原始日志另已复制到算例的 `audit-20260905-validation/`，不只保留在 `/tmp`。
实际 CLI 的高速均匀场检查以 `dt=0.01`、`attempts=1` 完成，而非先尝试输入的 `dt=0.1`。

### 后续工作顺序（未作为本批完成项）

1. 在不改变显式/隐式离散含义的前提下补全热、组分、黏性及声学时间尺度；补时间步低于下限时的提案值记录。
2. 复用节点内通信器，分开计算/输出/证据记录的墙钟时间，设置资源和证据采样周期；checkpoint 持久化语义单独保留。
3. 对同一有效物理时间段重测 PISO/SIMPLE/普通可压缩 COAST 各一轮；当前空间后备是稳定性措施，并非达成 COAST 性能目标。
4. 测量 halo 控制归约、Krylov 本地点积和 MG 刷新的独立成本，再决定分块 multidot 与 prepared halo 的扩展范围。
5. 完善产品编译阶段单 rank arena 失败注入、完整峰值预算及生命周期复用证明；补 IBM/开边界 Jacobian 方向导数检查。
6. 找回原始文献归档，并为新网格/新 Evidence schema 建立独立的预注册发布策略，不改写旧证据为新结果。

## 未完成项

本台账持续更新。当前仍未完成长时间统计验收、端到端 PISO/SIMPLE/普通可压缩 COAST 新版性能排名、
完整内存预算、普通 app 的节点通信器复用和记录周期、完整步墙钟拆分、halo 控制通信汇总、
分块 multidot、全部 Jacobian 方向导数覆盖及发布策略迁移。除对流外的物理时间尺度也尚未补齐。
历史 literature receipt 依赖旧目录与外部归档，不能靠改路径/哈希或跳过检查伪装成已验收。
未经实测的优化候选仅是候选，不计为修复或性能收益。
