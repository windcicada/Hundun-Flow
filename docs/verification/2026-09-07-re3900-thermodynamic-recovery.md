# Re3900 热力学恢复：局部证据与验收记录

基线：`b5b6bd619d138caaa6de03b5da89489fd272d211`。本文件随本轮验收更新。
本记录不是长测通过证明；未完成全部接受条件前不发布、不恢复生产长测。

## 已复现的独立缺陷

### 原生空气近参考焓反算

公开入口：`ThermodynamicsPlan::evaluate()`，Re3900 原生空气 NASA 参数、
273.15–6000 K、原始 `1e-12 / 200` 反算设置。
输入 `p=107136.1006180746 Pa, h=7 J/kg, hint=273.2472993645976 K`：

- RED：合法焓返回 `numerical_failure/804`。
- 固定容量诊断显示迭代 200 次，最后夹逼温度
  `[273.15692955196033, 273.15692955196039] K`，两端是相邻 double；
  残差 `3.6854075347036996e-11 J/kg`。继续迭代无法插入新温度。
- GREEN：仅在相邻温度且正向焓确实夹住目标值时，选择误差较小的合法端点。
  端点焓间隔和被选端点残差均须在浮点界内。真正越界仍返回 804。
- 原生空气、两组分 NASA 混合物，上下端点、向内/向外相邻焓、近参考焓，
  缺省/好/坏初猜、迭代上限 1、失败输出不变及调用内零分配回归通过。

浮点界针对已存储的 NASA 系数，而不是物性拟合误差。每个焓多项式单项
最多 8 次舍入，随后有 5 次加法、气体常数和组分质量分数乘法、组分求和。
采用保守 `gamma_(32+Ns) = n*epsilon/(1-n*epsilon)` 乘加权绝对单项和，
包含形成正值界的舍入裕量。最终界为两个端点的求值界之和，另加
`max(cp_l,cp_u)*(T_u-T_l)` 和焓相减的舍入裕量。仅在相邻可表示温度
分支计算此界，不改变普通迭代容差、NASA 正向计算、物性边界或迭代上限。

这不是原运行第 7232 步具体失败焓的恢复值；原日志未保存该值，不能混同。

### IBM 占位物性进入流体热扩散

公开入口：Cartesian diffusion + `IbmEquationInterfacePlan::correct_zero_normal_diffusion()`。
原单元测试的非均匀正温度场，保持所有流体状态和系数不变，
仅将固体占位单元导热系数从 1 改为 2：

- RED：流体侧扩散率最大变化 `164.376`（该单元测试量纲）。
- 根因：切面仍沿用流体/固体系数的串联调和导通系数；原修正只替换温度值。
- 候选修复：完整移除 Cartesian 固体侧贡献，再以流体单元物性系数与
  原 link 温度重建构造切面贡献。正值有界的预测端点同样遵循此物性权威。
- GREEN：占位物性变化后的流体差异 `4.54747e-13`；已接受速率/目标算子差异 0；
  现有固定物性 donor 方向导数误差 `2.49793e-09`。

上述 Jv 是已有 donor 热扩散公共接口回归，不是完整耦合 Jacobian 验收；
MPI donor、变物性耦合相关回归已通过，生产重启验收仍需完成。

### IBM 占位焓发生演化

公开入口：`ProductDriver`；16³ 网格、测试库的 `cylinder_ascii.stl`
实际为立方体 STL，静止几何、初始 300 K / Ux=1 m/s、固定 dt=1e-5 s。
RED：推进 4 步，运行时掩码标记的固体占位单元焓最大漂移
`0.0003807665198110044 J/kg`。这不是 Re3900 原尺寸/原 dt 重放。
阶段追踪定位到预测器：第 1 步在 halo 前 `h` 已变化，固体内面通量与
非对流速率非零。修复使高阶预测、低阶回退、率装配及面通量共用冻结掩码。
流体侧 p/h 校正原本已有活动域屏蔽；未重复修改该部分。

追加的低阶回归独立触发流体焓限制，固体保留合法但不同的旧历史与源项。
仅修高阶时 RED 焓漂移约 `-9346.67 J/kg`；接入低阶端点后 GREEN，
`rho/h/组分/被动标量` 保持已接受值，既有流体配对质量审计保留。

1/2/4 ranks 的 ProductDriver 均匀场 4 步固体焓漂移均为 0。
原生空气非均匀流体温度保持不变，仅将合法占位温度 280 K 改为 370 K，
一步后流体全字段最大相对差异分别为 `2.06795e-25 / 1.32349e-23 / 3.30872e-24`。
这是小型公开接口回归，不代表生产圆柱网格空间精度验收。

## 已运行的相关回归

- Release：热力学、IBM 方程接口、IDP 正常/低阶分支；ProductDriver 占位
  1/2/4 ranks；真实 runner checkpoint 分配失败和日志末次关闭故障。
- 重启存储 1/2/4 ranks；非零统计样本的同方法→主动方法恢复→再次精确续算
  1/2/4 ranks；非均匀闭域质量目标与库存合法小差异的方法历史回归。
- 压力—能量失败回滚 1/2/4 ranks；IBM 开边界/周期面通量 1/2/4 ranks；
  热物性耦合、IBM 压力算子；同进程销毁/重建与恢复/Visit 交叉内存 profile 1/2/4 ranks。
- Clang Debug ASan+UBSan：相关 8 项通过（含 ProductDriver 占位 1/2/4 ranks）。
  LeakSanitizer 因系统 MPI 保留对象关闭；这不替代进程 RSS 预算。
- GitHub main 只读核对为 `e0fd326d3ed6cccd52f812c113712f34a08cec95`，
  是本轮基线的祖先；发布前仍须重新核对。未推送。

## 观测合同

- 普通 CLI 与 runner 共用失败上下文输出，数值首错与最终停止原因分别保留。
- 反算诊断为可选固定结构；失败单元在既有失败路径重取，无正常全场诊断分配。
- Snapshot 借用实际 EBTopology 活动掩码，不复制全场、不改变 checkpoint 格式。
- health 旧全域列保留；追加 fluid / solid_placeholder 区域数量、极值及 global cell ID。
  ID 为 `i + Nx*(j + Ny*k)`；空区域值为 NaN、ID 为 UINT64_MAX。
- 局部状态追踪默认关闭，最多两个全局单元、96 条本地记录，溢出显式计数。

真实 CLI 回归 `v04_thermo_failure_cli_mpi_2` 已通过（11.04 s）。
runner 和普通 CLI 分别覆盖 rank 0 / rank 1 的 stage 15 反算失败，
只输出一份 root 上下文，保留首错/末次/停止原因；源文件哈希不变，
失败步不生成新 checkpoint / 完成标记。该回归使用新制造的完整 V3
checkpoint：300 K 初态、单个流体单元注入合成历史源项、反算上限 3 次。
这是故障注入，不是原 7232 步的物理重放，不修改任何既有源 image 的格式或恢复标记。

该回归同时发现并锁定另一项报告缺口：闭域非周期分支仍执行耦合 C2 和
最终连续性/能量审计，但未启用候选 refinement 循环时，终止标记一直为 `none`。
正常接受步因此在证据编码中被拒绝（RED `2/10401`）。现在仅在全部最终审计
成功后将其标记为 `component_residuals_converged`，额外求解次数仍为 0。
没有改为 pressure-only 合同，也没有放松证据校验。runner、普通 CLI 的
正常输出及独立 `runtime` 校验 GREEN；临时定位日志已移除。

## 待完成的生产接受条件

最终候选干净构建、最新源码的相关 sanitizer / 资源复核及
7000→7250 连续短测尚未完成，不能据上述局部结果发布生产程序。
随后才可推送已验收 main，并从新的 7250 checkpoint 同方法精确续算至 35000。
未修改原运行目录、源 checkpoint 和统计附件；未降低残差或守恒门槛。
