# GTMC 主线开发记录

输入来自 `/home/wyf/code_dev/gtmc-hundun-handoff-20260909`。本地工作区为
`/home/wyf/code_dev/flow`，算例为 `/home/wyf/code_dev/cases/g`，证据为
`/home/wyf/code_dev/check`。算例沿用原网格、NASA 表、入口流量和数值门槛。

## Standards

G28 的 12 个提交具备 DCO，明确规范违规 0 项。G29 提升条件 1 项：
删除 `DEBUG-G29` 临时探针。设计建议 1 项：合并多处组分扩散系数算法。
后者属于维护性建议，独立修复的接纳以方程与验证结果为依据。

## Spec

审查发现 3 项缺口：原网格组分猜测收敛、非局部连续性 Jacobian、临时
探针清理。完整交接材料保存了原始失败证据。原网格单步、连续 short100、
medium3000 和参考焓偏移对照需由相同主线版本重新运行。

审查共计：Standards 1 项提升条件，Spec 3 项缺口；各轴的主要问题分别为
临时探针清理和原网格求解／Jacobian 完整性。

## 融合范围

基线 `f8b70b7` 与 G28 `0eb23ce` 的共同祖先为 `86542bb`。
融合保留 main 的反应、ESF、TCR、喷雾、源项及 Restart V4/V5 接线。
入口扩展在原 case wire 外包一层 version 19，内部继续携带反应／喷雾标志；
历史签名同时包含入口、热材料和原有模型组件。

G29 独立修复首先移植 NASA 反演 Newton polish，以及分配异常的集体状态
传播。NASA 反例在 G28 融合库上返回 dT=0、drho=0；隔离替换反演实现后
正负四个扰动全部通过。生产历史增加 `thermal-inverse-newton-polish-v1`。
组分猜测、AA3、总能量、面重建及其他 G29 组继续按依赖和反例审查接入。

初轮 250 项回归中，变物性标量 MPI 1/2/4 复现交接中的失败。
应用测试还保留“源码树包含任何 case.json 即失败”的旧假设，与现有反应／
喷雾夹具冲突；改为断言应用运行前后源码算例集合相同。
分配审计中的主动中止记为中止，后续独立复跑结果另记。

NASA 修复与融合后的 63 项检查全部通过，覆盖原失败的变物性标量 MPI
1/2/4、应用初始化／重启、case wire、原生反应／PaSR／ESF／TCR／喷雾。
本机 OpenMPI 的 unsigned MIN/MAX 独立合约在 4 ranks 通过。
初轮完整结果为 245/250；三项标量及应用测试已经复跑通过，一项主动中止的
immersed MPI2 分配扫描仍需完整复跑。

## 守恒算子融合

移植 G29 的组分携焓扩散、共享黏性面功、h 主变量总能量、配套时间导数、
材料／梯度 halo、IBM 物理组分率、拉伸网格限制器及 trace 率算术。
总能量 Product 路径先用于惰性单相算例；原有反应／喷雾接线继续使用原
能量路线。ESF 的 Gamma∇h 已包含组分焓输运，新增 Fourier 组分修正明确
跳过此路径，避免重复计算。外部客户端需重新编译，方法历史签名相应更新。

128 单元周期剪切流独立反例：旧库首步能量缺陷 −35.6578127939 W；
新算子 BE/BDF2 三步缺陷分别约 9.09e−8、−5.29e−8、1.06e−8 W，
总库存变化小于 9.2e−11 J。MPI 1/2/4 均通过。
73 项相关回归中 72 项首次通过；余下一项是合并后测试把 ESF 的 h 坐标
误当成 T 坐标，按独立 h 通量修正测试预期后单项通过。
此前中止的 immersed MPI2 分配扫描完整复跑通过，耗时 303.05 s。

同一库的生成焓接触反例仍在 16 次外耦合后失败（6/10216，残差
2.7443453e−12）。这作为下一组时间层／组分猜测修复的 RED，保留原门槛。

## 目标时间组分

等温生成焓反例在守恒算子版本上达到 16 次外耦合上限；单独接入目标时间
组分 BDF 方程及等温 h/Y 未提交猜测后，MPI1 一次耦合通过，残差
8.65e−17、相对质量误差 4.64e−16、相对能量误差 5.00e−16。
MPI2/4 也通过。目标时间路线仅用于惰性总能量模式；反应／喷雾保持原有
源项与标量配对。G29 AA3 加速及其历史存储已从本次移植中剔除；
`DEBUG-G29` 探针全部清理，32eps、128 次内迭代及 16 次外耦合上限保留。

G28/NASA 版本原网格单步运行约 16 分钟后主动中止，期间没有接受步骤或
新检查点。该次记录作为中止诊断，后续主线计算从重新导入的 step20000
独立启动。连续短中测均使用同一个版本的独立窗口。

目标时间组分后的 93 项检查首次 90 项通过，三个开放边界测试仍使用旧 EX
预测／remap 的预算公式。按目标时间的边界 Y 与面流量独立计算通量后，
正向／回流 MPI1/2/4 全部通过；被动标量保留原预算公式。另加三组分、
不同 cp、生成焓参考偏置的 BE/BDF2 等温接触验证，MPI1/2/4 全部通过。
相应六项回归总耗时 2.85 s。CSV 同步输出组分携焓扩散独立预算项。

## 微量组分诊断

融合后的完整 focused/scalar 套件 262/262 通过（422.56 s）。原网格
单步在五次尝试后均返回 10217，dt 从 4.4948431655e−7 降到
2.8092769785e−8 s；该次运行未产生接受步骤或新检查点。

局部组分团块的 96×4×4 周期反例确认 Jacobi 每次传播一个单元，
128 次后的相对残差为 1.48e−7。局部扩散扫掠试验加快传播后，
暴露 TVD 端点算术的约 6e−14 残差平台。独立面值测试在原算子上失败：
MC 端点以 donor + (neighbor − donor) 计算，放大微量邻居的舍入误差。
端点分支改为等价加权表达式，同步直接及缓存方向导数；限制器和存储
精度保持原值。面值测试转绿。扫掠仍作为外部试验，当前主线采用原组分
迭代方法。新历史签名标记端点算术，原网格将从均值数据重新导入验证。

端点修复后的 35 项标量／面算子回归及 13 项 Schur／总能量／时间收敛回归全部通过。


## 原网格接受与重启动

程序提交 `3bae1f2a2b0463ac7a9779d260076f564a15ff89`，64 ranks，原输入条件。
程序 SHA-256：`8176ff9de27bdc979eeee0d18fb798cdb1e7e4a0b2089344aa329f000daf6693`。
重新导入 V1 的字段／通量读回误差均为零。首步在三次 10217 减步后接受，
并写出 V3 检查点；随后从该检查点精确重启动，两步均保持 BDF2 并直接接受。

| step | BDF | dt / s | 重试 | 独立库存差分质量缺陷 / kg s⁻¹ | 独立库存差分总能量缺陷 / W |
|---|---|---|---|---|---|
| 20001 | BE | 5.6185539569e−8 | 3 | −2.43e−15 | 1.2142e−4 |
| 20002 | BDF2 | 6.7422647483e−8 | 0 | 3.91e−12 | −2.8785e−4 |
| 20003 | BDF2 | 8.0907176979e−8 | 0 | 4.94e−12 | −6.8045e−4 |

库存由独立读取检查点后，按 NASA 物性和流体体积重新累加，再使用实际
BE／变步长 BDF2 系数求差分；面通量预算取同一步 diagnostics 的物理收支项。
这一区分保留了独立库存计算与产品面预算的各自来源。CH4 库存 BDF 率为
6.96666666670e−4、6.96666666688e−4、6.96666666692e−4 kg/s，
与规定燃料流量 6.966666667e−4 kg/s 一致。

step20003 的流体单元数为 3,901,529，T=289.882–298.514 K，
p=90.356–107.443 kPa，最大速度 182.904 m/s；所有 p/T/rho/U/Y 有限，
组分范围和组分和检查通过。V3 读回方法签名为 `11261292420827948325`。
三步内核耗时约 434.3、262.6、354.2 s；首步含减步开销，后续两步期间还有
独立读回作业，耗时用于记录本次调试成本，不作为隔离性能基准。

工作目录采用 `/home/wyf/code_dev/cases/g`，复核程序及日志位于同级 `check`。
`run.sh` 核验上述程序哈希后，从同一 V1 起点启动独立连续 short100，
结果写入 `short`。短测正在运行，既有三步与短测分别记录；medium3000、
原网格焓参考不变性及完整连续性对流密度 Jacobian 仍为后续验证／开发项目。

实际融合后的 Standards／Spec 复查确认原生源项接线和端点导数覆盖完整。
方法历史组件及 API 历史温度／证书迁移表已补齐；同一 trace 端点反例增加
直接与缓存方向导数检查，制造解测试通过。README 保持原内容。


## 短测成本与面通量复用

2026-09-09 检查 `short` 前 8 个接受步骤。首步保留原来的三次减步；
后续步骤直接接受。8 步累计计算 4045.65 s，物理时间推进 9.27010e−7 s。

| step | dt / s | 单步耗时 / s | 线性迭代数 | 阻塞归约数 |
|---|---|---|---|---|
| 20001 | 5.61855e−8 | 433.02 | 908 | 32992 |
| 20002 | 6.74226e−8 | 254.15 | 583 | 20922 |
| 20003 | 8.09072e−8 | 353.06 | 899 | 29287 |
| 20004 | 9.70886e−8 | 379.37 | 995 | 30923 |
| 20005 | 1.16506e−7 | 455.44 | 1219 | 37219 |
| 20006 | 1.39808e−7 | 617.19 | 1576 | 48587 |
| 20007 | 1.67769e−7 | 708.38 | 1931 | 57537 |
| 20008 | 2.01323e−7 | 845.04 | 2453 | 67927 |

第 8 步 EOS、连续性、能量终端残差分别为 6.55e−16、1.34e−15、
1.72e−13，规定门槛均为 1e−10。8 步质量收支缺陷绝对值最大
4.11e−12 kg/s，总能量收支缺陷最大 1.19e−3 W。当前主要限制是计算
吞吐率；这些短时检查仍需结合更长时间和参考不变性验证。

同一 `re/Restart`、同一 dt=9.708861237538154e−8 s、同一组 64 ranks
重放 step20004，原程序需要 9 轮组分耦合、146 次标量内迭代。每轮均重做
压力—能量校正；64 个详细记录槽之外还有 17 次求解，汇总计时包含全部求解。

组分携焓对流和扩散的面值在相邻单元间重复计算。现在使用 8×8×8 局部块内
共享面值，栈上暂存小于 2 KiB；每次调用重新计算，单元内加减顺序和 IBM
切面修正保持原有顺序。偏移子域的逐面独立对照检查面值逐位一致，完整域
面计算数由 9180 降为 5506，屏蔽固体后由 7218 降为 4956。

| 同起点重放 | 原程序 | 面值复用 |
|---|---|---|
| 单步耗时 / s | 404.894 | 389.637 |
| 候选态局部计算 / s | 135.645 | 116.172 |
| 组分耦合轮数 / 标量内迭代 | 9 / 146 | 9 / 146 |
| 详细记录内的线性迭代数 | 64 次记录 | 逐次相同 |

此计时与原短测并行，复用版本的开头还运行了小型回归；属于本机调试测量。
面值复用处理重复局部计算，完整连续性对流密度 Jacobian 和组分外耦合
成本仍属于后续工作。

重启动回归同时复现了原程序的 `reacting-esf-ibm` 启动错误 14504：
结构化 halo 新增 T 后有 8 个字段，ESF donor 计划仍登记 h/标量/随机场/
transport 共 7 个字段。修正使 donor 交换显式使用该已登记前缀，T 继续由
历史温度的结构化 halo 路径提供。

最终相关回归 86/86 通过（52.98 s），包括总能量、组分预算、EOS、IBM 面算子、
回退、时间收敛、历史签名及 12 组耦合 CLI 重启动。共享面单元测试覆盖偏移
子域、分块边缘、固体屏蔽、跨调用输入变化和面计算失败。已结束原网格同起点
单步重放；原 short100 进程继续使用 `3bae1f2` 版本独立运行。
