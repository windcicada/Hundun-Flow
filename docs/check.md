# 0.1.0 交付检查

检查日期：2026-09-23。客户端基于界面提交 `c8c6a7c` 建立独立 `client` 分支。

| 范围 | 结果 |
|---|---|
| 已有 API、进程身份、原生控制与绘图检查 | 复用原有 27 项并通过 |
| SQLite 持久化、旧记录迁移、请求去重、版本保存 | 通过 |
| 求解器缺席时的启动、结果接口与模型设置 | 通过 |
| 真实 AsyncSSH/SFTP、辅助程序部署、重连与主机公钥核对 | 通过 |
| 固定 SDK、MCP 工具、流式模型响应、会话持久化 | 通过确定性模型检查 |
| 本机及 SSH 小算例：新算、输出、保存停止、独立目录恢复 | 通过；2 MPI、8×8×8 网格 |
| 实际场的绘图、报告、检查点归档 | 通过 |
| AI 完整流程 | 确定性兼容端点驱动实际 SDK/MCP，完成准备、检查、新算、续算、绘图、报告、归档 |
| 桌面／手机页面 | 1440×1000、390×844 检查通过；页面脚本错误为 0 |
| Windows | 已加入 CI 的安装、API 与浏览器检查；本地交付环境为 Linux |
| 用户模型服务 | 待配置真实端点与凭据；设置页面及任务恢复入口已接通 |

本次只使用独立小算例，计算端程序使用外部发行包。所有试运行目录和检查点保存在本地 `check/`，源码包保留复现脚本。小算例检查确认客户端交互闭环；物理模型精度、生产网格与长期统计由求解器验收负责。

常规检查共 37 项；后续局部检查复用已有结果，仅重跑改动覆盖项。安装 wheel 在仓库外目录成功启动，预构建页面与求解器缺席时的 API 正常。源码包和 wheel 的名称、内容及许可检查通过。

联调修正了短场文件名的分区归组、大写原生物理量映射、MPI 重复点的截面插值及状态文件最终发布与进程退出之间的读取时序。SSH 检查期间一次外部 MPI 原生预检返回 finalize 异常，客户端记录并拒绝该次派发；后续独立预检及恢复正常完成。

安装包包含预构建网页、客户端代码和许可。程序版本为 0.1.0，运行时通过选择的路径连接外部求解器。多用户服务、调度器及桌面外壳按后续计划推进。
