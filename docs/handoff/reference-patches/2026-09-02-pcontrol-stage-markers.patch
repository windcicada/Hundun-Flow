diff --git a/versions/v0.4/src/core_product_freeze.cpp b/versions/v0.4/src/core_product_freeze.cpp
index a3e94a9..d7aafa8 100644
--- a/versions/v0.4/src/core_product_freeze.cpp
+++ b/versions/v0.4/src/core_product_freeze.cpp
@@ -2556,0 +2557,3 @@ void ProductDriver::Impl::reset_stage_timings(StageId stage) noexcept {
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+  (void)MPI_Pcontrol(static_cast<int>(stage));
+#endif
@@ -2571,0 +2575,3 @@ void ProductDriver::Impl::begin_timed_stage(StageId stage) noexcept {
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+  (void)MPI_Pcontrol(static_cast<int>(stage));
+#endif
@@ -11822,0 +11829,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+        (void)MPI_Pcontrol(corrector == 1U ? 141 : 151);
+#endif
@@ -11826,0 +11836,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+        (void)MPI_Pcontrol(corrector == 1U ? 40 : 50);
+#endif
@@ -11894,0 +11907,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+            (void)MPI_Pcontrol(corrector == 1U ? 142 : 152);
+#endif
@@ -11897,0 +11913,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+            (void)MPI_Pcontrol(corrector == 1U ? 40 : 50);
+#endif
@@ -12003,0 +12022,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+        (void)MPI_Pcontrol(corrector == 1U ? 143 : 153);
+#endif
@@ -12008,0 +12030,3 @@ Status ProductDriver::Impl::execute_attempt(
+#if defined(HUNDUN_V04_PERF_STAGE_PCONTROL)
+        (void)MPI_Pcontrol(corrector == 1U ? 40 : 50);
+#endif
