# DCO

本项目使用 Developer Certificate of Origin 约束提交来源。提交者只能提交自己有权贡献、并允许项目按当前许可证分发的内容。

合规提交需要由提交者本人添加：

```text
Signed-off-by: Your Name <your.email@example.com>
```

常用命令：

```sh
git commit -s
```

`Signed-off-by` 是来源声明，不是代签手续。自动化工具不得替用户生成、补写或冒用该行。发现缺失时，应由原提交者确认后自行修正。
